# 智扫通 · 扫地机器人智能客服 Agent
# Python 3.13
#
# 版本号依据本项目虚拟环境实测（pip list），非估计值。

# ---------- 代码中直接 import 的第三方库 ----------
langchain==1.3.15
langchain-core==1.5.5
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
langgraph==1.2.11
streamlit==1.62.0
PyYAML==6.0.3

# ---------- 底层 SDK：代码未直接 import，但功能强依赖 ----------
# dashscope -> ChatTongyi / DashScopeEmbeddings 的底层 SDK
dashscope==1.27.0
# chromadb  -> langchain-chroma 的向量库后端
chromadb==1.5.9
# pypdf     -> langchain-community 的 PyPDFLoader 依赖；缺失时 PDF 加载会失败
pypdf==6.16.1
